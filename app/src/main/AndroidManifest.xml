<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络权限（Wi-Fi 检测） -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 无障碍服务（系统级别，不需要 uses-permission，通过设置开启） -->
    <!-- 悬浮窗权限 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 后台启动 App -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- 通知权限（Android 13+） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 忽略电池优化（需要用户主动授权） -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- 前台服务 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- 开机启动（让 WorkManager 在重启后重新调度） -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- 唤醒锁（WorkManager + 点亮屏幕） -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 精确闹钟权限（Android 12+ 使用 setAlarmClock 需要） -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- 禁用锁屏（需要系统签名或使用 root） -->
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />

    <!-- Android 11+ 包可见性：声明需要与钉钉交互 -->
    <queries>
        <!-- 查询钉钉是否已安装，获取启动 Intent -->
        <package android:name="com.alibaba.android.rimet" />
        <!-- 通过 Intent 方式查询（兜底） -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".AutoClockApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoClock">

        <!-- 主 Activity：任务列表 -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 任务编辑页 -->
        <activity
            android:name=".ui.edit.TaskEditActivity"
            android:exported="false"
            android:label="@string/title_task_edit"
            android:parentActivityName=".ui.main.MainActivity"
            android:windowSoftInputMode="adjustResize" />

        <!-- 权限引导页 -->
        <activity
            android:name=".ui.permission.PermissionGuideActivity"
            android:exported="false"
            android:label="@string/title_permission_guide" />

        <!-- 无障碍服务声明 -->
        <service
            android:name=".service.AutoClockAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- WorkManager 初始化（Hilt 注入） -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- 开机广播接收器 -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Wi-Fi 广播接收器 -->
        <receiver
            android:name=".receiver.WifiReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.wifi.STATE_CHANGE" />
            </intent-filter>
        </receiver>

        <!-- 闹钟广播接收器（AlarmManager 精确触发） -->
        <receiver
            android:name=".worker.ClockAlarmReceiver"
            android:exported="false" />

        <!-- 通知 Channel 元数据 -->
        <meta-data
            android:name="com.example.autoclock.NOTIFICATION_CHANNEL_ID"
            android:value="auto_clock_channel" />
    </application>

</manifest>
